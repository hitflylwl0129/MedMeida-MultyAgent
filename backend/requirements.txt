# Web / 编排
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.1
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
# FastAPI 处理 multipart/form-data（文件上传）必需，选品 Agent v2.0 销量表上传用
python-multipart>=0.0.9

# 编排状态机（Runtime 文档：单进程 LangGraph 起步）
langgraph>=0.2.0

# 腾讯云点播 SDK（CreateAigcVideoTask / DescribeTaskDetail）
tencentcloud-sdk-python-vod>=3.0.1300

# 医生形象图上传到 VOD（可选：本地文件直传需要）
# 若只用 URL 拉取上传(PullUpload) 可不装；本地文件直传需要它
vod-python-sdk>=1.4.4

# 话术 Agent LLM 客户端（OpenAI 兼容端点：腾讯云 lkeap / hy3-preview）
openai>=1.40

# 可灵 Kling 原厂 API（路 A：image2video + advanced-lip-sync）
PyJWT>=2.8
requests>=2.31

# 腾讯云 Agent Runtime（选品 Agent 沙箱，E2B 协议兼容）
# 文档：https://cloud.tencent.com/document/product/1814
e2b-code-interpreter>=1.0
e2b>=1.0
# 浏览器沙箱通过 CDP 远程操控（本端只装 SDK，chromium 跑在沙箱里，无需 install_browser）
playwright>=1.45

